**Tool Mapping for OpenCode:**

- `list_planets` — OpenCode MCP tool: `list_planets`
- `get_agent_context(topic, query)` — OpenCode MCP tool: `get_agent_context`
- `add_note(topic, kind, content)` — OpenCode MCP tool: `add_note`
- `update_planet(topic, current_state, next_step)` — OpenCode MCP tool: `update_planet`
- `log_turn(topic, content)` — OpenCode MCP tool: `log_turn`
- `search_nodes(query, limit)` — OpenCode MCP tool: `search_nodes`
- `search_notes(topic, kind, query, limit)` — OpenCode MCP tool: `search_notes`
- `get_node(node_id)` — OpenCode MCP tool: `get_node`
- `read_planet(topic)` — OpenCode MCP tool: `read_planet`
