**Tool Mapping for Gemini CLI:**

- `list_planets` — Gemini MCP tool: `list_planets`
- `get_agent_context(topic, query)` — Gemini MCP tool: `get_agent_context`
- `add_note(topic, kind, content)` — Gemini MCP tool: `add_note`
- `update_planet(topic, current_state, next_step)` — Gemini MCP tool: `update_planet`
- `log_turn(topic, content)` — Gemini MCP tool: `log_turn`
- `search_nodes(query, limit)` — Gemini MCP tool: `search_nodes`
- `search_notes(topic, kind, query, limit)` — Gemini MCP tool: `search_notes`
- `get_node(node_id)` — Gemini MCP tool: `get_node`
- `read_planet(topic)` — Gemini MCP tool: `read_planet`
